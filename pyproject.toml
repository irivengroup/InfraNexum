[tool.coverage.run]
source = ["validation.architecture", "validation.migrations", "validation.toolchains", "validation.eventing", "validation.persistence"]
branch = true

[tool.coverage.report]
fail_under = 98
show_missing = true
skip_covered = false
