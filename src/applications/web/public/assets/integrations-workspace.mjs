import { applyTranslations, localeFromDocument, translate } from './i18n.mjs';
import { initializeEnterpriseDataTables, refreshEnterpriseDataTable } from './enterprise-crud.mjs';
import { JiraAssetsClient } from './jira-assets.mjs';
import { NotificationClient } from './integration-notifications.mjs';
import { ConnectorGovernanceClient } from './connector-governance.mjs';
import { ConnectorSyncClient } from './connector-sync.mjs';
import { ServiceNowClient } from './service-now.mjs';

/** PGM-10-E06 UI for governed providers, federated reads and durable outbound notifications. */
export async function initializeIntegrationsWorkspace(documentObject = document, configuration, fetchFunction = fetch) {
  const root = documentObject?.getElementById?.('integrations-workspace');
  if (!root) return Object.freeze({ enabled: false });
  const enabled = configuration?.integrationsConnectorsEnabled === true;
  root.setAttribute('data-capability-enabled', String(enabled));
  if (!enabled) return Object.freeze({ enabled: false });

  root.innerHTML = template();
  applyTranslations(documentObject, localeFromDocument(documentObject));
  const jira = new JiraAssetsClient(configuration, { fetchFunction });
  const serviceNow = new ServiceNowClient(configuration, { fetchFunction });
  const notifications = new NotificationClient(configuration, { fetchFunction });
  const governance = new ConnectorGovernanceClient(configuration, { fetchFunction });
  const sync = new ConnectorSyncClient(configuration, { fetchFunction });
  wire(documentObject, jira, serviceNow, notifications, governance, sync);
  initializeEnterpriseDataTables(root);

  const refresh = async () => {
    const [policies] = await Promise.all([
      refreshGovernance(documentObject, governance),
      refreshProvider(documentObject, jira, 'jira-assets'),
      refreshProvider(documentObject, serviceNow, 'service-now'),
      refreshNotifications(documentObject, notifications),
    ]);
    await refreshSync(documentObject, sync, policies);
  };
  try { await refresh(); setStatus(documentObject, 'integrations.status.ready', 'success'); }
  catch (error) { setStatus(documentObject, 'integrations.status.unavailable', 'danger', error?.message); }
  return Object.freeze({ enabled: true, refresh });
}

export function integrationsWorkspaceTemplate() { return template(); }

function template() {
  return `<header class="d-flex flex-wrap justify-content-between align-items-start gap-3 mb-4"><div><p class="small text-uppercase fw-bold text-primary mb-1" data-i18n="integrations.eyebrow">Integrations</p><h2 id="integrations-workspace-title" data-i18n="integrations.title">Connector providers</h2><p data-i18n="integrations.description">Governed external providers with explicit authority and sync direction.</p></div><button id="integrations-refresh" class="btn btn-outline-primary" type="button" data-i18n="common.refresh">Refresh</button></header>
<p id="integrations-status" class="alert alert-info py-2" role="status" aria-live="polite" data-state="info" data-i18n="integrations.status.loading">Loading connectors…</p>
${governanceSection()}
${syncSection()}
${providerSection('jira-assets','integrations.jira.title','integrations.jira.federated')}
<section class="border rounded p-3 bg-body-tertiary mb-4"><h3 class="h5" data-i18n="integrations.search.title">Federated AQL read</h3><p class="small text-body-secondary" data-i18n="integrations.search.description">Query Jira Assets without copying provider attributes.</p><form id="jira-assets-search" class="row g-3"><div class="col-md-4"><label class="form-label" for="jira-assets-connector" data-i18n="integrations.connector">Connector</label><select id="jira-assets-connector" class="form-select" required></select></div><div class="col-md-6"><label class="form-label" for="jira-assets-aql" data-i18n="integrations.aql">AQL</label><input id="jira-assets-aql" class="form-control" maxlength="4096" required autocomplete="off" placeholder="objectType = Server"></div><div class="col-md-2 d-flex align-items-end"><button class="btn btn-primary w-100" type="submit" data-i18n="integrations.search">Search</button></div></form><div class="table-responsive mt-3"><table class="table table-hover align-middle"><thead><tr><th data-i18n="integrations.objectKey">Object key</th><th data-i18n="integrations.label">Label</th><th data-i18n="integrations.objectType">Object type</th><th data-i18n="integrations.remoteId">Remote ID</th></tr></thead><tbody id="jira-assets-results"></tbody></table></div><p id="jira-assets-page" class="small text-body-secondary mb-0" aria-live="polite"></p></section>
${providerSection('service-now','integrations.servicenow.title','integrations.servicenow.federated')}
<section class="border rounded p-3 bg-body-tertiary mb-4"><h3 class="h5" data-i18n="integrations.servicenow.searchTitle">Federated CMDB read</h3><p class="small text-body-secondary" data-i18n="integrations.servicenow.searchDescription">Search the selected ServiceNow CMDB without copying provider attributes.</p><form id="service-now-search" class="row g-3"><div class="col-md-4"><label class="form-label" for="service-now-connector" data-i18n="integrations.connector">Connector</label><select id="service-now-connector" class="form-select" required></select></div><div class="col-md-6"><label class="form-label" for="service-now-query" data-i18n="integrations.servicenow.query">CI name</label><input id="service-now-query" class="form-control" maxlength="256" required autocomplete="off" placeholder="server"></div><div class="col-md-2 d-flex align-items-end"><button class="btn btn-primary w-100" type="submit" data-i18n="integrations.search">Search</button></div></form><div class="table-responsive mt-3"><table class="table table-hover align-middle"><thead><tr><th data-i18n="integrations.servicenow.name">Name</th><th data-i18n="integrations.servicenow.class">Class</th><th data-i18n="integrations.remoteId">Remote ID</th><th data-i18n="integrations.servicenow.updated">Updated</th></tr></thead><tbody id="service-now-results"></tbody></table></div><p id="service-now-page" class="small text-body-secondary mb-0" aria-live="polite"></p></section>
${notificationSection()}`;
}

function governanceSection() {
  return `<section class="mb-4" aria-labelledby="connector-governance-title"><div class="mb-2"><h3 id="connector-governance-title" class="h5 mb-1" data-i18n="integrations.governance.title">Connector governance</h3><p class="small text-body-secondary mb-0" data-i18n="integrations.governance.description">Authority · synchronization direction · conflicts · deletion policy · rollback contract</p></div><div class="table-responsive"><table class="table table-hover align-middle"><thead><tr><th data-i18n="integrations.connector">Connector</th><th data-i18n="integrations.provider">Provider</th><th data-i18n="integrations.direction">Direction</th><th data-i18n="integrations.authority">Authority</th><th data-i18n="integrations.governance.conflicts">Conflicts</th><th data-i18n="integrations.governance.deletions">Deletions</th><th data-i18n="integrations.governance.rollback">Rollback</th><th data-inx-action-column data-i18n="common.actions">Actions</th></tr></thead><tbody id="connector-governance-policies"></tbody></table></div><p id="connector-governance-page" class="small text-body-secondary mb-0" aria-live="polite"></p></section>`;
}

function syncSection() {
  return `<section class="mb-4" aria-labelledby="connector-sync-title"><div class="mb-2"><h3 id="connector-sync-title" class="h5 mb-1" data-i18n="integrations.sync.title">Synchronization runtime</h3><p class="small text-body-secondary mb-0" data-i18n="integrations.sync.description">Durable runs · append-only checkpoints · explicit resume · governed compensation</p></div><div id="connector-sync-readonly" class="alert alert-secondary py-2" data-i18n="integrations.sync.noMutator">No mutating connector is currently approved. Jira Assets and ServiceNow remain federated read-only.</div><form id="connector-sync-execute" class="row g-3 border rounded p-3 bg-body-tertiary mb-3"><div class="col-lg-4"><label class="form-label" for="connector-sync-connector" data-i18n="integrations.connector">Connector</label><select id="connector-sync-connector" class="form-select" required></select></div><div class="col-lg-3"><label class="form-label" for="connector-sync-direction" data-i18n="integrations.direction">Direction</label><select id="connector-sync-direction" class="form-select" required><option>INBOUND</option><option>OUTBOUND</option><option>BIDIRECTIONAL</option></select></div><div class="col-lg-2"><label class="form-label" for="connector-sync-max-batches" data-i18n="integrations.sync.maxBatches">Max batches</label><input id="connector-sync-max-batches" class="form-control" type="number" min="1" max="100" value="10" required></div><div class="col-lg-3"><label class="form-label" for="connector-sync-reason" data-i18n="integrations.sync.reason">Reason</label><input id="connector-sync-reason" class="form-control" minlength="3" maxlength="500" required autocomplete="off"></div><div class="col-12 d-flex justify-content-end"><button id="connector-sync-execute-button" class="btn btn-primary" type="submit" data-i18n="integrations.sync.execute">Execute</button></div></form><div class="table-responsive"><table class="table table-hover align-middle"><thead><tr><th data-i18n="integrations.connector">Connector</th><th data-i18n="integrations.provider">Provider</th><th data-i18n="integrations.direction">Direction</th><th data-i18n="common.status">Status</th><th data-i18n="integrations.sync.revision">Revision</th><th data-i18n="integrations.governance.rollback">Rollback</th><th data-inx-action-column data-i18n="common.actions">Actions</th></tr></thead><tbody id="connector-sync-runs"></tbody></table></div><p id="connector-sync-page" class="small text-body-secondary mb-0" aria-live="polite"></p><div class="table-responsive mt-3"><table class="table table-sm align-middle"><thead><tr><th data-i18n="integrations.sync.revision">Revision</th><th data-i18n="integrations.sync.kind">Kind</th><th data-i18n="integrations.sync.cursorHash">Cursor hash</th><th data-i18n="integrations.sync.processed">Processed</th><th data-i18n="integrations.sync.changed">Changed</th><th data-i18n="integrations.sync.rejected">Rejected</th></tr></thead><tbody id="connector-sync-checkpoints"></tbody></table></div></section>`;
}

function notificationSection() {
  return `<section class="mb-4" aria-labelledby="notification-endpoints-title"><div class="mb-2"><h3 id="notification-endpoints-title" class="h5 mb-1" data-i18n="integrations.notifications.title">Operational notifications</h3><p class="small text-body-secondary mb-0" data-i18n="integrations.notifications.description">Durable signed webhooks · InfraNexum authority · explicit delivery targets</p></div><div class="table-responsive"><table class="table table-hover align-middle"><thead><tr><th data-i18n="integrations.notifications.endpoint">Endpoint</th><th data-i18n="integrations.notifications.transport">Transport</th><th data-i18n="integrations.direction">Direction</th><th data-i18n="integrations.authority">Authority</th><th data-i18n="common.status">Status</th><th data-inx-action-column data-i18n="common.actions">Actions</th></tr></thead><tbody id="notification-endpoints"></tbody></table></div></section>
<section class="border rounded p-3 bg-body-tertiary mb-4"><h3 class="h5" data-i18n="integrations.notifications.publishTitle">Publish operational event</h3><form id="notification-publish" class="row g-3"><div class="col-lg-4"><label class="form-label" for="notification-endpoint" data-i18n="integrations.notifications.endpoint">Endpoint</label><select id="notification-endpoint" class="form-select" required></select></div><div class="col-lg-4"><label class="form-label" for="notification-event-id" data-i18n="integrations.notifications.eventId">Event ID</label><input id="notification-event-id" class="form-control" minlength="8" maxlength="200" required autocomplete="off" placeholder="evt-20260817-0001"></div><div class="col-lg-4"><label class="form-label" for="notification-event-type" data-i18n="integrations.notifications.eventType">Event type</label><input id="notification-event-type" class="form-control" minlength="3" maxlength="128" required autocomplete="off" placeholder="infrastructure.health.changed"></div><div class="col-12"><label class="form-label" for="notification-payload" data-i18n="integrations.notifications.payload">JSON payload</label><textarea id="notification-payload" class="form-control font-monospace" rows="5" maxlength="1048576" required>{}</textarea></div><div class="col-12 d-flex justify-content-end"><button class="btn btn-primary" type="submit" data-i18n="integrations.notifications.publish">Publish</button></div></form></section>
<section class="border rounded p-3 bg-body-tertiary"><div class="d-flex flex-wrap justify-content-between gap-2 align-items-center"><div><h3 class="h5 mb-1" data-i18n="integrations.notifications.dlqTitle">Notification dead-letter queue</h3><p class="small text-body-secondary mb-0" data-i18n="integrations.notifications.dlqDescription">Inspect metadata and explicitly replay failed deliveries. Payload bodies are not exposed.</p></div><button id="notification-dlq-refresh" type="button" class="btn btn-sm btn-outline-primary" data-i18n="common.refresh">Refresh</button></div><div class="table-responsive mt-3"><table class="table table-hover align-middle"><thead><tr><th data-i18n="integrations.notifications.deliveryId">Delivery ID</th><th data-i18n="integrations.notifications.endpoint">Endpoint</th><th data-i18n="integrations.notifications.eventType">Event type</th><th data-i18n="integrations.notifications.attempts">Attempts</th><th data-i18n="integrations.notifications.failure">Failure</th><th data-inx-action-column data-i18n="common.actions">Actions</th></tr></thead><tbody id="notification-dlq"></tbody></table></div><p id="notification-dlq-page" class="small text-body-secondary mb-0" aria-live="polite"></p></section>`;
}

function providerSection(id,titleKey,federatedKey){return `<section class="mb-4"><div class="mb-2"><h3 class="h5 mb-1" data-i18n="${titleKey}">${id}</h3><p class="small text-body-secondary mb-0" data-i18n="${federatedKey}">Federated read · external authority · no implicit import</p></div><div class="table-responsive"><table class="table table-hover align-middle"><thead><tr><th data-i18n="integrations.connector">Connector</th><th data-i18n="integrations.provider">Provider</th><th data-i18n="integrations.direction">Direction</th><th data-i18n="integrations.authority">Authority</th><th data-i18n="common.status">Status</th><th data-inx-action-column data-i18n="common.actions">Actions</th></tr></thead><tbody id="${id}-connectors"></tbody></table></div></section>`;}

function wire(doc,jira,sn,notifications,governance,sync){
  doc.getElementById('integrations-refresh')?.addEventListener('click',async()=>{try{await Promise.all([refreshProvider(doc,jira,'jira-assets'),refreshProvider(doc,sn,'service-now'),refreshGovernance(doc,governance),refreshNotifications(doc,notifications)]);setStatus(doc,'integrations.status.ready','success');}catch(e){setStatus(doc,'integrations.status.unavailable','danger',e?.message);}});
  doc.getElementById('jira-assets-search')?.addEventListener('submit',async e=>{e.preventDefault();try{setStatus(doc,'integrations.status.searching','info');const r=await jira.search(doc.getElementById('jira-assets-connector')?.value,doc.getElementById('jira-assets-aql')?.value,{limit:50});renderJira(doc,r.payload,r.pagination);setStatus(doc,'integrations.status.ready','success');}catch(x){setStatus(doc,'integrations.status.searchFailed','danger',x?.message);}});
  doc.getElementById('service-now-search')?.addEventListener('submit',async e=>{e.preventDefault();try{setStatus(doc,'integrations.servicenow.searching','info');const r=await sn.search(doc.getElementById('service-now-connector')?.value,doc.getElementById('service-now-query')?.value,{limit:50});renderServiceNow(doc,r.payload,r.pagination);setStatus(doc,'integrations.status.ready','success');}catch(x){setStatus(doc,'integrations.servicenow.searchFailed','danger',x?.message);}});
  doc.getElementById('notification-publish')?.addEventListener('submit',async e=>{e.preventDefault();try{const raw=doc.getElementById('notification-payload')?.value??'';const payload=JSON.parse(raw);await notifications.publish(doc.getElementById('notification-event-id')?.value,doc.getElementById('notification-event-type')?.value,[doc.getElementById('notification-endpoint')?.value],payload);setStatus(doc,'integrations.notifications.published','success');await refreshNotifications(doc,notifications);}catch(x){setStatus(doc,'integrations.notifications.publishFailed','danger',x?.message);}});
  doc.getElementById('notification-dlq-refresh')?.addEventListener('click',async()=>{try{await refreshNotificationDlq(doc,notifications);setStatus(doc,'integrations.status.ready','success');}catch(x){setStatus(doc,'integrations.notifications.dlqFailed','danger',x?.message);}});
  doc.getElementById('connector-sync-execute')?.addEventListener('submit',async e=>{e.preventDefault();try{const key=doc.getElementById('connector-sync-connector')?.value;const reason=doc.getElementById('connector-sync-reason')?.value;await sync.execute(key,{direction:doc.getElementById('connector-sync-direction')?.value,maxBatches:Number(doc.getElementById('connector-sync-max-batches')?.value??10),reason,idempotencyKey:operatorIdempotency('execute',key)});setStatus(doc,'integrations.sync.started','success');await refreshSync(doc,sync,[]);}catch(x){setStatus(doc,'integrations.sync.failed','danger',x?.message);}});
}

async function refreshProvider(doc,client,id){const r=await client.connectors();const items=Array.isArray(r.payload)?r.payload:[];const tbody=doc.getElementById(`${id}-connectors`);tbody?.replaceChildren(...items.map(item=>connectorRow(doc,client,item,id)));if(tbody?.closest)refreshEnterpriseDataTable(tbody.closest('table'));const select=doc.getElementById(`${id}-connector`);if(select){select.replaceChildren(...items.filter(x=>x.enabled).map(x=>option(doc,x.connectorKey)));select.disabled=select.options.length===0;}}
function connectorRow(doc,client,item,id){const row=doc.createElement('tr');for(const v of [item.connectorKey,item.provider,item.direction,item.authority])append(row,v);const status=doc.createElement('td');status.textContent=item.enabled?translate(localeFromDocument(doc),'common.enabled'):translate(localeFromDocument(doc),'common.disabled');row.appendChild(status);const actions=doc.createElement('td');const b=button(doc,'integrations.health','btn btn-sm btn-outline-primary');b.disabled=!item.enabled;b.addEventListener('click',async()=>{try{const r=await client.health(item.connectorKey);setStatus(doc,r.payload?.status==='UP'?(id==='service-now'?'integrations.servicenow.healthUp':'integrations.health.up'):'integrations.status.unavailable',r.payload?.status==='UP'?'success':'danger');}catch(e){setStatus(doc,id==='service-now'?'integrations.servicenow.healthDown':'integrations.health.down','danger',e?.message);}});actions.appendChild(b);row.appendChild(actions);return row;}

async function refreshGovernance(doc,client){
  const r=await client.policies({limit:200});
  const items=Array.isArray(r.payload)?r.payload:[];
  const tbody=doc.getElementById('connector-governance-policies');
  tbody?.replaceChildren(...items.map(item=>governanceRow(doc,client,item)));
  if(tbody?.closest)refreshEnterpriseDataTable(tbody.closest('table'));
  page(doc,'connector-governance-page',items,r.pagination);
  return items;
}
function governanceRow(doc,client,item){
  const row=doc.createElement('tr');
  for(const v of [item.connectorKey,item.provider,item.direction,item.authority,item.conflictStrategy,item.deletionPolicy,item.rollbackStrategy])append(row,v);
  const actions=doc.createElement('td');
  const plan=button(doc,'integrations.governance.dryRun','btn btn-sm btn-outline-primary');
  plan.addEventListener('click',async()=>{
    try{
      const fields=Array.isArray(item.fields)?item.fields.map(field=>field.field).filter(Boolean):[];
      const r=await client.plan(item.connectorKey,{direction:item.direction,fields,propagateDeletions:false});
      const allowed=r.payload?.decision==='ALLOW';
      const detail=allowed?`${item.connectorKey} · ${r.payload?.rollbackStrategy??item.rollbackStrategy}`:(Array.isArray(r.payload?.reasons)?r.payload.reasons.join(' · '):item.connectorKey);
      setStatus(doc,allowed?'integrations.governance.allowed':'integrations.governance.denied',allowed?'success':'warning',detail);
    }catch(e){setStatus(doc,'integrations.governance.failed','danger',e?.message);}
  });
  actions.appendChild(plan); row.appendChild(actions); return row;
}

async function refreshSync(doc,client,policies=[]){
  const mutating=(Array.isArray(policies)?policies:[]).filter(item=>['INBOUND','OUTBOUND','BIDIRECTIONAL'].includes(item.direction));
  const select=doc.getElementById('connector-sync-connector'); const execute=doc.getElementById('connector-sync-execute-button'); const notice=doc.getElementById('connector-sync-readonly');
  if(select){select.replaceChildren(...mutating.map(item=>option(doc,item.connectorKey)));select.disabled=mutating.length===0;} if(execute)execute.disabled=mutating.length===0;if(notice)notice.hidden=mutating.length>0;
  const result=await client.runs({limit:50});const items=Array.isArray(result.payload)?result.payload:[];const tbody=doc.getElementById('connector-sync-runs');tbody?.replaceChildren(...items.map(item=>syncRunRow(doc,client,item,policies)));if(tbody?.closest)refreshEnterpriseDataTable(tbody.closest('table'));page(doc,'connector-sync-page',items,result.pagination);
}
function syncRunRow(doc,client,item,policies=[]){const row=doc.createElement('tr');for(const v of [item.connectorKey,item.provider,item.direction,item.status,item.lastCheckpointRevision,item.rollbackStrategy])append(row,v);const actions=doc.createElement('td');const checkpoints=button(doc,'integrations.sync.checkpoints','btn btn-sm btn-outline-primary me-1');checkpoints.addEventListener('click',async()=>{try{const r=await client.checkpoints(item.connectorKey,{limit:50});renderSyncCheckpoints(doc,r.payload);setStatus(doc,'integrations.status.ready','success');}catch(e){setStatus(doc,'integrations.sync.failed','danger',e?.message);}});actions.appendChild(checkpoints);if(item.status==='PAUSED'){const resume=button(doc,'integrations.sync.resume','btn btn-sm btn-outline-secondary me-1');resume.addEventListener('click',async()=>{try{const reason=syncOperatorReason(doc);await client.resume(item.runId,reason,operatorIdempotency('resume',item.runId));setStatus(doc,'integrations.sync.resumed','success');await refreshSync(doc,client,policies);}catch(e){setStatus(doc,'integrations.sync.failed','danger',e?.message);}});actions.appendChild(resume);}if(['SUCCEEDED','FAILED'].includes(item.status)&&item.rollbackStrategy!=='NONE_REQUIRED'){const compensate=button(doc,'integrations.sync.compensate','btn btn-sm btn-outline-danger');compensate.addEventListener('click',async()=>{try{const reason=syncOperatorReason(doc);await client.compensate(item.runId,reason,operatorIdempotency('compensate',item.runId));setStatus(doc,'integrations.sync.compensated','success');await refreshSync(doc,client,policies);}catch(e){setStatus(doc,'integrations.sync.failed','danger',e?.message);}});actions.appendChild(compensate);}row.appendChild(actions);return row;}
function syncOperatorReason(doc){const value=doc.getElementById('connector-sync-reason')?.value;const reason=String(value??'').trim();if(reason.length<3||reason.length>500)throw new Error('Connector synchronization operator reason must contain 3 to 500 characters');return reason;}
function renderSyncCheckpoints(doc,items){const rows=Array.isArray(items)?items:[];const tbody=doc.getElementById('connector-sync-checkpoints');tbody?.replaceChildren(...rows.map(item=>{const row=doc.createElement('tr');for(const v of [item.revision,item.kind,item.cursorSha256,item.processedCount,item.changedCount,item.rejectedCount])append(row,v);return row;}));if(tbody?.closest)refreshEnterpriseDataTable(tbody.closest('table'));}
function operatorIdempotency(operation,subject){const entropy=globalThis.crypto?.randomUUID?.()??`${Date.now()}-${Math.random().toString(16).slice(2)}`;return `web-${operation}-${String(subject??'connector')}-${entropy}`.slice(0,200);}

async function refreshNotifications(doc,client){const r=await client.endpoints({limit:200});const items=Array.isArray(r.payload)?r.payload:[];const tbody=doc.getElementById('notification-endpoints');tbody?.replaceChildren(...items.map(item=>notificationEndpointRow(doc,client,item)));if(tbody?.closest)refreshEnterpriseDataTable(tbody.closest('table'));const select=doc.getElementById('notification-endpoint');if(select){select.replaceChildren(...items.filter(x=>x.enabled).map(x=>option(doc,x.endpointKey)));select.disabled=select.options.length===0;}await refreshNotificationDlq(doc,client);}
function notificationEndpointRow(doc,client,item){const row=doc.createElement('tr');for(const v of [item.endpointKey,item.transport,item.direction,item.authority])append(row,v);const status=doc.createElement('td');status.textContent=item.enabled?translate(localeFromDocument(doc),'common.enabled'):translate(localeFromDocument(doc),'common.disabled');row.appendChild(status);const actions=doc.createElement('td');const runtime=button(doc,'integrations.notifications.runtime','btn btn-sm btn-outline-primary me-1');runtime.disabled=!item.enabled;runtime.addEventListener('click',async()=>{try{const r=await client.runtime(item.endpointKey);setStatus(doc,r.payload?.suspended?'integrations.notifications.suspended':'integrations.notifications.healthy',r.payload?.suspended?'warning':'success',`${translate(localeFromDocument(doc),'integrations.notifications.backlog')}: ${r.payload?.backlog??0}`);}catch(e){setStatus(doc,'integrations.status.unavailable','danger',e?.message);}});const resume=button(doc,'integrations.notifications.resume','btn btn-sm btn-outline-secondary');resume.disabled=!item.enabled;resume.addEventListener('click',async()=>{try{await client.resume(item.endpointKey,'operator recovery');setStatus(doc,'integrations.notifications.resumed','success');await refreshNotifications(doc,client);}catch(e){setStatus(doc,'integrations.notifications.resumeFailed','danger',e?.message);}});actions.append(runtime,resume);row.appendChild(actions);return row;}
async function refreshNotificationDlq(doc,client){const r=await client.deadLetters({limit:50});const items=Array.isArray(r.payload)?r.payload:[];const tbody=doc.getElementById('notification-dlq');tbody?.replaceChildren(...items.map(item=>notificationDeadLetterRow(doc,client,item)));if(tbody?.closest)refreshEnterpriseDataTable(tbody.closest('table'));page(doc,'notification-dlq-page',items,r.pagination);}
function notificationDeadLetterRow(doc,client,item){const row=doc.createElement('tr');for(const v of [item.deliveryId,item.endpointKey,item.eventType,item.attempts,item.failureCode])append(row,v);const actions=doc.createElement('td');const replay=button(doc,'integrations.notifications.replay','btn btn-sm btn-outline-primary');replay.addEventListener('click',async()=>{try{await client.replay(item.deliveryId,'operator replay');setStatus(doc,'integrations.notifications.replayed','success');await refreshNotificationDlq(doc,client);}catch(e){setStatus(doc,'integrations.notifications.replayFailed','danger',e?.message);}});actions.appendChild(replay);row.appendChild(actions);return row;}

function renderJira(doc,items,p){renderRows(doc,'jira-assets-results',items,x=>[x.objectKey,x.label,x.objectTypeName,x.id]);page(doc,'jira-assets-page',items,p);}
function renderServiceNow(doc,items,p){renderRows(doc,'service-now-results',items,x=>[x.name,x.className,x.sysId,x.updatedOn]);page(doc,'service-now-page',items,p);}
function renderRows(doc,id,items,map){const rows=Array.isArray(items)?items:[];const tbody=doc.getElementById(id);tbody?.replaceChildren(...rows.map(item=>{const r=doc.createElement('tr');for(const v of map(item))append(r,v);return r;}));if(tbody?.closest)refreshEnterpriseDataTable(tbody.closest('table'));}
function page(doc,id,items,p){const n=Array.isArray(items)?items.length:0;const node=doc.getElementById(id);if(node){const next=p?.nextOffset==null?'—':`${translate(localeFromDocument(doc),'integrations.page.next')} ${p.nextOffset}`;node.textContent=`${n} · ${next}`;}}
function option(doc,value){const o=doc.createElement('option');o.value=value;o.textContent=value;return o;}
function button(doc,key,className){const b=doc.createElement('button');b.type='button';b.className=className;b.textContent=translate(localeFromDocument(doc),key);return b;}
function append(row,value){const c=row.ownerDocument.createElement('td');c.textContent=value??'—';row.appendChild(c);}
function setStatus(doc,key,context,detail=''){const n=doc.getElementById('integrations-status');if(!n)return;const base=translate(localeFromDocument(doc),key);n.textContent=detail?`${base} ${detail}`:base;n.className=`alert alert-${context} py-2`;n.setAttribute('data-state',context);}
