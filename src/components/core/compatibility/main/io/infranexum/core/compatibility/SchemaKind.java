package io.infranexum.core.compatibility;

/** Categories governed by the Core Contracts schema registry. */
public enum SchemaKind {
    API,
    EVENT,
    COMMAND,
    IMPORT,
    EXPORT,
    RSOT_CANONICAL,
    RSOT_EXTENSION,
    CUSTOM_FIELD,
    PROJECTION
}
